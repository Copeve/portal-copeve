import database from '../models';

class ConcursoController{
    /*static async pegaTodosOsConcursos (req: Request, res: Response){
        try {
            const todosOsConcursos= await database.Concursos.findAll();
            res.status(200).json(todosOsConcursos);
        } catch (error) {
            res.status(500).json(error);
        }
    }*/
}
export default ConcursoController;